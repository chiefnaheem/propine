{
  "name": "blockchain-asset",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "index": "dist/index.js",
  "scripts": {
    "start": "yarn build && node dist/index.js",
    "start:dev": "ts-node src/index.ts",
    "clean": "rm -rf dist && mkdir dist",
    "build": "tsc",
    "test": "jest"
  },
  "dependencies": {
    "cryptocompare": "^1.0.0",
    "csv-parser": "^3.0.0",
    "jest": "^29.3.1",
    "moment": "^2.29.4",
    "request-promise-native": "^1.0.9"
  },
  "devDependencies": {
    "@types/jest": "^29.2.4",
    "@types/request-promise-native": "^1.0.18"
  }
}
